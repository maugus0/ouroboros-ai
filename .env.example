# API Configuration — ouroboros-ai
# Copy to .env.local and update values

# Backend API Base URL
VITE_API_BASE_URL=http://localhost:8000

# API Version (optional, defaults to v1)
VITE_API_VERSION=v1
